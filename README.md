# Introduce
BlockPJDBC is sub project for BlockP Projects. 

# Features  
>function to connect to database by jdbc driver(auto load ojdbc8.jar/sqlite-jdbc.jar/postgresql.jar in ../libs/)  
>project type jdbc  
>format register sql/jdbclink(.bpjdbc)  
>function to load db structure cache in jdbc project  
>transformer for blob>byte[]  
>transformer for clob>String  
